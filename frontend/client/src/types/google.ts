/**
 * Runtime side-effect import for Google API types.
 * The actual type declarations live in google.d.ts (ambient types).
 * This file exists so that `import '../../types/google'` resolves at runtime.
 */
export {};
