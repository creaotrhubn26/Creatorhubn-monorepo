/**
 * Admin email designer presets — re-exports from shared module.
 * The canonical data lives in shared/email-presets.ts so the backend can also import it.
 */
export {
  ADMIN_EMAIL_DESIGNER_PRESETS,
  type EmailPreset as AdminEmailDesignerPreset,
} from '../../../../shared/email-presets';
