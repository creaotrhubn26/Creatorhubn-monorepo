/**
 * FEATURE_COMPONENT_MAP
 * Maps high-level platform feature IDs to the component IDs that
 * implement them in the UniversalComponentRegistry.
 *
 * This file is intentionally small and hand-curated. Add entries here
 * gradually as you wire concrete components to features.
 */

export const FEATURE_COMPONENT_MAP: Record<string, string[]> = {
  // Example mapping – replace with real component IDs as you wire things up:
  // 'universal-showcase': ['universal-showcase-root'],
};

