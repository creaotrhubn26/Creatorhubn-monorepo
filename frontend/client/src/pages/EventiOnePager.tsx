import React from 'react';
import { Box, Typography } from '@mui/material';

/**
 * EventiOnePager — Evendi one-page overview.
 * Stub component to resolve import in App.tsx.
 */
const EventiOnePager: React.FC = () => {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4">Evendi One-Pager</Typography>
      <Typography variant="body1" sx={{ mt: 2 }}>
        Evendi event integration overview — coming soon.
      </Typography>
    </Box>
  );
};

export default EventiOnePager;
