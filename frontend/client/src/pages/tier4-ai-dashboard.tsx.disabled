import React from 'react';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";
import { 
  Bot,
  Zap,
  Building,
  Workflow,
  Brain,
  TrendingUp,
  Settings,
  Target,
  Users,
  Database,
  Cloud,
  Shield,
  Activity,
  Calendar,
  FileText,
  MessageSquare,
  CreditCard,
  BarChart3,
  Lightbulb,
  Sparkles,
  Clock
} from 'lucide-react';

export default function Tier4AIDashboard() {
  const tier4Tools = [
    {
      id: '1',
      name: 'AI Arbeidsflyt Automatisering',
      description: 'Intelligente arbeidsflyter for norske kreative profesjonelle med avansert AI-drevet automatisering',
      icon: Bot,
      color: 'from-purple-600 to-blue-600',
      path: '/ai-workflow-automation',
      features: [
        'Bryllupsfoto automatisering',
        'Sosiale medier publiseringsflyt', 
        'Videoredigering batch-prosessering',
        'Klientoppfølging og fakturering',
        'Musikkmiks automatisering',
        'Portfolio backup og sync'
      ],
      complexity: 'advanced',
      status: 'active',
      executionCount: 349,
      timeSaved: '24.5 timer'
    },
    {
      id: '2', 
      name: 'Enterprise Integrasjonsplattform',
      description: 'Sømløs integrasjon med norske bedriftssystemer og avanserte enterprise-tjenester',
      icon: Building,
      color: 'from-blue-600 to-cyan-600',
      path: '/enterprise-integration-platform',
      features: [
        'Tripletex Regnskap integrasjon',
        'HubSpot CRM synkronisering',
        'Dropbox Business lagring',
        'Mailchimp Marketing kampanjer',
        'Slack Workspace kommunikasjon',
        'Google Analytics 4 rapporter'
      ],
      complexity: 'enterprise',
      status: 'connected',
      dataPoints: '79k+',
      syncSuccess: '97.8%'
    }
  ];

  const tierProgression = [
    {
      tier: 'Tier 1',
      name: 'Forretningsverktøy',
      tools: ['Prosjekt Timeline', 'Fakturaoppretting', 'Kontraktmaler', 'Utstyrvedlikehold'],
      status: 'completed',
      icon: FileText,
      color: 'bg-green-100 text-green-800'
    },
    {
      tier: 'Tier 2', 
      name: 'Arbeidsflytforbedring',
      tools: ['Sosiale Medier Planlegger', 'Foto Batch Prosessor'],
      status: 'completed',
      icon: Workflow,
      color: 'bg-blue-100 text-blue-800'
    },
    {
      tier: 'Tier 3',
      name: 'Avansert Analyse',
      tools: ['Business Intelligence Dashboard', 'Ytelsesoptimaliserer'],
      status: 'completed',
      icon: TrendingUp,
      color: 'bg-purple-100 text-purple-800'
    },
    {
      tier: 'Tier 4',
      name: 'AI Workflow Automatisering',
      tools: ['AI Arbeidsflyt System', 'Enterprise Integrasjon'],
      status: 'active',
      icon: Bot,
      color: 'bg-gradient-to-r from-purple-600 to-blue-600 text-white'
    }
  ];

  const aiMetrics = {
    totalWorkflows: 6,
    activeIntegrations: 8,
    timeSaved: 24.5,
    automationSuccess: 94.7,
    dataProcessed: '79.1k',
    costSavings: '€2,847'
  };

  const navigateToTool = (path: string) => {
    console.log(`Navigating to Tier 4 AI tool: ${path}`);
    window.location.href = path;
  };

  return (
    <div className="min-h-screen bg-gradient-to-br from-slate-900 via-blue-900 to-slate-900 p-4 sm:p-6 relative overflow-hidden">
      {/* Enhanced Glassmorphism Floating Orbs */}
      <div className="absolute inset-0 overflow-hidden pointer-events-none">
        <div className="absolute top-1/4 left-1/4 w-96 h-96 bg-gradient-to-r from-purple-400/30 to-blue-500/20 rounded-full blur-3xl animate-pulse"></div>
        <div className="absolute bottom-1/3 right-1/4 w-80 h-80 bg-gradient-to-r from-cyan-400/25 to-teal-500/15 rounded-full blur-3xl animate-pulse delay-1000"></div>
        <div className="absolute top-1/2 right-1/3 w-64 h-64 bg-gradient-to-r from-amber-400/20 to-orange-500/10 rounded-full blur-3xl animate-pulse delay-2000"></div>
        <div className="absolute bottom-1/4 left-1/3 w-72 h-72 bg-gradient-to-r from-emerald-400/15 to-green-500/10 rounded-full blur-3xl animate-pulse delay-3000"></div>
        <div className="absolute top-3/4 left-1/2 w-56 h-56 bg-gradient-to-r from-pink-400/25 to-purple-500/15 rounded-full blur-3xl animate-pulse delay-4000"></div>
      </div>

      <div className="max-w-7xl mx-auto relative z-10 space-y-8">
      {/* Header */}
      <div className="text-center mb-12">
        <div className="flex items-center justify-center mb-4">
          <div className="p-4 bg-gradient-to-r from-purple-600 to-blue-600 rounded-full mr-4">
            <Bot className="w-12 h-12 text-white" />
          </div>
          <div className="text-left">
            <h1 className="text-4xl font-bold bg-gradient-to-r from-purple-600 to-blue-600 bg-clip-text text-transparent">
              Tier 4: AI Workflow Automatisering
            </h1>
            <p className="text-xl text-gray-600 mt-2">
              Høyeste nivå av intelligent automatisering for norske kreative profesjonelle
            </p>
          </div>
        </div>
        
        <Badge className="bg-gradient-to-r from-purple-600 to-blue-600 text-white px-6 py-2 text-lg">
          <Sparkles className="w-5 h-5 mr-2" />
          Enterprise AI Automation
        </Badge>
      </div>

      {/* AI Metrics Overview */}
      <div className="grid grid-cols-1 md:grid-cols-3 lg:grid-cols-6 gap-6 mb-12">
        <Card className="bg-gradient-to-br from-purple-50 to-blue-50">
          <CardHeader className="pb-2">
            <CardTitle className="text-sm font-medium text-purple-800 flex items-center gap-2">
              <Workflow className="w-4 h-4" />
              AI Arbeidsflyter
            </CardTitle>
          </CardHeader>
          <CardContent>
            <div className="text-2xl font-bold text-purple-900">{aiMetrics.totalWorkflows}</div>
            <div className="text-sm text-purple-600">aktive</div>
          </CardContent>
        </Card>

        <Card className="bg-gradient-to-br from-blue-50 to-cyan-50">
          <CardHeader className="pb-2">
            <CardTitle className="text-sm font-medium text-blue-800 flex items-center gap-2">
              <Building className="w-4 h-4" />
              Integrasjoner
            </CardTitle>
          </CardHeader>
          <CardContent>
            <div className="text-2xl font-bold text-blue-900">{aiMetrics.activeIntegrations}</div>
            <div className="text-sm text-blue-600">tilkoblet</div>
          </CardContent>
        </Card>

        <Card className="bg-gradient-to-br from-green-50 to-emerald-50">
          <CardHeader className="pb-2">
            <CardTitle className="text-sm font-medium text-green-800 flex items-center gap-2">
              <Clock className="w-4 h-4" />
              Tid spart
            </CardTitle>
          </CardHeader>
          <CardContent>
            <div className="text-2xl font-bold text-green-900">{aiMetrics.timeSaved}t</div>
            <div className="text-sm text-green-600">denne uken</div>
          </CardContent>
        </Card>

        <Card className="bg-gradient-to-br from-amber-50 to-orange-50">
          <CardHeader className="pb-2">
            <CardTitle className="text-sm font-medium text-amber-800 flex items-center gap-2">
              <Target className="w-4 h-4" />
              Suksessrate
            </CardTitle>
          </CardHeader>
          <CardContent>
            <div className="text-2xl font-bold text-amber-900">{aiMetrics.automationSuccess}%</div>
            <div className="text-sm text-amber-600">automatisering</div>
          </CardContent>
        </Card>

        <Card className="bg-gradient-to-br from-indigo-50 to-purple-50">
          <CardHeader className="pb-2">
            <CardTitle className="text-sm font-medium text-indigo-800 flex items-center gap-2">
              <Database className="w-4 h-4" />
              Data prosessert
            </CardTitle>
          </CardHeader>
          <CardContent>
            <div className="text-2xl font-bold text-indigo-900">{aiMetrics.dataProcessed}</div>
            <div className="text-sm text-indigo-600">datapunkter</div>
          </CardContent>
        </Card>

        <Card className="bg-gradient-to-br from-teal-50 to-cyan-50">
          <CardHeader className="pb-2">
            <CardTitle className="text-sm font-medium text-teal-800 flex items-center gap-2">
              <CreditCard className="w-4 h-4" />
              Besparelser
            </CardTitle>
          </CardHeader>
          <CardContent>
            <div className="text-2xl font-bold text-teal-900">{aiMetrics.costSavings}</div>
            <div className="text-sm text-teal-600">månedlig</div>
          </CardContent>
        </Card>
      </div>

      {/* Tier Progression */}
      <div className="mb-12">
        <h2 className="text-2xl font-bold text-gray-800 mb-6 text-center">CreatorHub Norge Tier Progresjon</h2>
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
          {tierProgression.map((tier, index) => {
            const Icon = tier.icon;
            return (
              <Card key={tier.tier} className={`${tier.status === 'active' ? 'ring-2 ring-purple-600 shadow-lg' : ''}`}>
                <CardHeader>
                  <div className="flex items-center justify-between">
                    <Badge className={tier.color}>
                      <Icon className="w-4 h-4 mr-2" />
                      {tier.tier}
                    </Badge>
                    {tier.status === 'completed' && (
                      <Badge className="bg-green-100 text-green-800">
                        Fullført
                      </Badge>
                    )}
                    {tier.status === 'active' && (
                      <Badge className="bg-gradient-to-r from-purple-600 to-blue-600 text-white">
                        <Sparkles className="w-3 h-3 mr-1" />
                        Aktiv
                      </Badge>
                    )}
                  </div>
                  <CardTitle className="text-lg">{tier.name}</CardTitle>
                </CardHeader>
                <CardContent>
                  <div className="space-y-2">
                    {tier.tools.map(tool => (
                      <div key={tool} className="text-sm text-gray-600 flex items-center">
                        <span className="w-2 h-2 bg-gray-400 rounded-full mr-2"></span>
                        {tool}
                      </div>
                    ))}
                  </div>
                </CardContent>
              </Card>
            );
          })}
        </div>
      </div>

      {/* Tier 4 AI Tools */}
      <div className="mb-12">
        <h2 className="text-2xl font-bold text-gray-800 mb-6 flex items-center">
          <Bot className="w-8 h-8 mr-3 text-purple-600" />
          Tier 4 AI Automatiseringsverktøy
        </h2>
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-8">
          {tier4Tools.map(tool => {
            const Icon = tool.icon;
            return (
              <Card key={tool.id} className="hover:shadow-xl transition-all duration-300 border-2 hover:border-purple-200">
                <CardHeader>
                  <div className="flex items-center justify-between mb-4">
                    <div className={`p-3 bg-gradient-to-r ${tool.color} rounded-lg`}>
                      <Icon className="w-8 h-8 text-white" />
                    </div>
                    <Badge className={
                      tool.complexity === 'enterprise' ? 'bg-gradient-to-r from-purple-600 to-blue-600 text-white' :
                      tool.complexity === 'advanced' ? 'bg-amber-100 text-amber-800' : 'bg-blue-100 text-blue-800'
                    }>
                      {tool.complexity === 'enterprise' ? 'Enterprise' : 
                       tool.complexity === 'advanced' ? 'Avansert' : 'Standard'}
                    </Badge>
                  </div>
                  <CardTitle className="text-xl mb-2">{tool.name}</CardTitle>
                  <CardDescription className="text-base">{tool.description}</CardDescription>
                </CardHeader>
                <CardContent>
                  <div className="space-y-6">
                    {/* Features */}
                    <div>
                      <h4 className="font-semibold text-gray-800 mb-3">Hovedfunksjoner:</h4>
                      <div className="grid grid-cols-1 gap-2">
                        {tool.features.map(feature => (
                          <div key={feature} className="flex items-center text-sm text-gray-600">
                            <span className="w-2 h-2 bg-purple-500 rounded-full mr-3"></span>
                            {feature}
                          </div>
                        ))}
                      </div>
                    </div>

                    {/* Stats */}
                    <div className="bg-gray-50 rounded-lg p-4">
                      <div className="grid grid-cols-2 gap-4">
                        {tool.executionCount && (
                          <div>
                            <span className="text-sm text-gray-500">Kjøringer:</span>
                            <div className="font-bold text-purple-600">{tool.executionCount}</div>
                          </div>
                        )}
                        {tool.timeSaved && (
                          <div>
                            <span className="text-sm text-gray-500">Tid spart:</span>
                            <div className="font-bold text-green-600">{tool.timeSaved}</div>
                          </div>
                        )}
                        {tool.dataPoints && (
                          <div>
                            <span className="text-sm text-gray-500">Datapunkter:</span>
                            <div className="font-bold text-blue-600">{tool.dataPoints}</div>
                          </div>
                        )}
                        {tool.syncSuccess && (
                          <div>
                            <span className="text-sm text-gray-500">Suksessrate:</span>
                            <div className="font-bold text-green-600">{tool.syncSuccess}</div>
                          </div>
                        )}
                      </div>
                    </div>

                    {/* Action Button */}
                    <Button 
                      onClick={() => navigateToTool(tool.path)}
                      className={`w-full bg-gradient-to-r ${tool.color} hover:opacity-90 text-white font-semibold py-3`}
                    >
                      <Zap className="w-5 h-5 mr-2" />
                      Åpne {tool.name}
                    </Button>
                  </div>
                </CardContent>
              </Card>
            );
          })}
        </div>
      </div>

      {/* AI Automation Benefits */}
      <Card className="bg-gradient-to-r from-purple-50 to-blue-50 border-purple-200">
        <CardHeader>
          <CardTitle className="text-2xl text-center text-purple-800 flex items-center justify-center">
            <Lightbulb className="w-8 h-8 mr-3" />
            Fordeler med Tier 4 AI Automatisering
          </CardTitle>
        </CardHeader>
        <CardContent>
          <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            <div className="text-center">
              <div className="p-4 bg-white rounded-full w-16 h-16 mx-auto mb-4 flex items-center justify-center">
                <Clock className="w-8 h-8 text-purple-600" />
              </div>
              <h4 className="font-semibold mb-2">Tidsbesparelse</h4>
              <p className="text-sm text-gray-600">Automatiserer repetitive oppgaver og frigjør tid til kreativt arbeid</p>
            </div>
            <div className="text-center">
              <div className="p-4 bg-white rounded-full w-16 h-16 mx-auto mb-4 flex items-center justify-center">
                <Target className="w-8 h-8 text-blue-600" />
              </div>
              <h4 className="font-semibold mb-2">Høyere kvalitet</h4>
              <p className="text-sm text-gray-600">Konsistente resultater og redusert risiko for menneskelige feil</p>
            </div>
            <div className="text-center">
              <div className="p-4 bg-white rounded-full w-16 h-16 mx-auto mb-4 flex items-center justify-center">
                <TrendingUp className="w-8 h-8 text-green-600" />
              </div>
              <h4 className="font-semibold mb-2">Skalerbarhet</h4>
              <p className="text-sm text-gray-600">Håndter flere prosjekter samtidig uten proporsjonalt arbeid</p>
            </div>
          </div>
        </CardContent>
      </Card>
    </div>
  );
}