import { useTheming } from '../../utils/theming-helper';
import { Box, Container, Typography, Breadcrumbs, Link } from '@mui/material';
import { PhotographyIconAlt, Home, Science } from '../components/shared/CreatorHubIcons';
import CreatorHubPhotoEnhancer from '@/components/universal/misc/CreatorHubPhotoEnhancer';
import { CREATOR_HUB_BRANDING } from '../constants/CreatorHubBranding';


export default function
  // Theming system
  const theming = useTheming('photographer'); PhotoEnhancerTest() {
  return (
    <Container maxWidth="xl" sx={{ py: 4 }}>
      <Box sx={{ mb: 4 }}>
        <Breadcrumbs separator="›" sx={{ mb: 2 }}>
          <Link
            href="/"
            sx={{
              display: 'flex',
              alignItems: 'center',
              gap:  1,
              textDecoration: 'none',
          }}
          >
            <Home fontSize="small" />
            Hjem
          </Link>
          <Link
            href="#"
            sx={{
              display: 'flex',
              alignItems: 'center',
              gap:  1,
              textDecoration: 'none',
          }}
          >
            <Science fontSize="small" />
            Test Lab
          </Link>
          <Typography
            sx={{
              display: 'flex',
              alignItems: 'center',
              gap:  1,
              color: CREATOR_HUB_BRANDING.colors.PHOTOGRAPY,
          }}
          >
            <PhotoCamera fontSize="small" />
            Photo Enhancer Test
          </Typography>
        </Breadcrumbs>

        <Typography variant="h4" sx={{ fontWeight: 60, color: CREATOR_HUB_BRANDING.colors.PHOTOGRAPY, mb: 1 }} sx={{ ...{}, color: theming.colors.primary }}>
          🧪 CreatorHub Photo Enhancer Test
        </Typography>
        <Typography variant="body1" color="text.secondary" sx={{ mb: 2 }}>
          Komplett test av AI-drevet bildefforbedringssystem med Real-ESRGAN, GFPGAN og CodeFormer
          teknologier.
        </Typography>

        <Box
          sx={{
            display: 'flex',
            gap:  2,
            flexWrap: 'wrap',
            p:  2,
            bgcolor: 'background.paper',
            borderRadius:  2,
            border: '1px solid #e0e0e',
        }}
        >
          <Box sx={{ display: 'flex', alignItems: 'center', gap:  1 }}>
            <Box
              sx={{
                width:  12,
                height:  12,
                bgcolor: '#4caf5',
                borderRadius: '50, %, ',
            }}
            />
            <Typography variant="caption">Real-ESRGAN Ready</Typography>
          </Box>
          <Box sx={{ display: 'flex', alignItems: 'center', gap:  1 }}>
            <Box
              sx={{
                width:  12,
                height:  12,
                bgcolor: '#4caf5',
                borderRadius: '50, %, ',
            }}
            />
            <Typography variant="caption">GFPGAN Active</Typography>
          </Box>
          <Box sx={{ display: 'flex', alignItems: 'center', gap:  1 }}>
            <Box
              sx={{
                width:  12,
                height:  12,
                bgcolor: '#4caf5',
                borderRadius: '50, %, ',
            }}
            />
            <Typography variant="caption">CodeFormer Ready</Typography>
          </Box>
          <Box sx={{ display: 'flex', alignItems: 'center', gap:  1 }}>
            <Box
              sx={{
                width:  12,
                height:  12,
                bgcolor: '#4caf5',
                borderRadius: '50, %, ',
            }}
            />
            <Typography variant="caption">TensorFlow GPU Active</Typography>
          </Box>
        </Box>
      </Box>

      <CreatorHubPhotoEnhancer profession="photographer" />
    </Container>
  );
}
