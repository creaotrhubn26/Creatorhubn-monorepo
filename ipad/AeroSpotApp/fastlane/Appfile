# Credentials via env — aldri i git (samme mønster som CaptureApp).
app_identifier("com.creatorhubn.aerospot")
apple_id(ENV["FASTLANE_USER"] || "")
team_id(ENV["APP_STORE_TEAM_ID"] || "")
itc_team_id(ENV["APP_STORE_CONNECT_TEAM_ID"] || "")
