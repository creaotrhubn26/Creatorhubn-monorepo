name: Auto-migrate Neon-DB ved migrations-endring

# Appliserer SQL-migrasjoner mot prod-Neon via /api/admin-room/migrations/run
# (token-gated m/ MIGRATE_TRIGGER_TOKEN). migrate.sh er idempotent (sporet i
# `_migrations_applied`), fortsetter-på-feil per fil, og hopper over drizzle-push
# på Render (SQL er autoritativt).
#
# 🔑 DEPLOY-RACE LØST: den gamle push-triggeren fyrte SAMTIDIG som Render-
# autoDeployen → curl-en traff backenden midt i rolloveren → 502 + løsrevet
# migrate.sh drept av SIGTERM. NÅ venter workflowen først til /api/version
# rapporterer at NØYAKTIG denne commit-en er utplassert og stabil, FØR den
# trigger migrate. Da kjører migrate.sh mot en stabil instans.

on:
  push:
    branches: [main]
    paths:
      - "backend/migrations/**"
  workflow_dispatch:
    # Manuell nødluke for én eksplisitt migreringsfil mot gjeldende main.
    inputs:
      migration_file:
        description: "Eksakt .sql-fil (kjøres alene)"
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: auto-migrate-prod
  cancel-in-progress: false

env:
  BACKEND_URL: https://creatorhub-backend-rtbl.onrender.com
  RENDER_SERVICE_ID: srv-d76ob60ule4c73dv2p60
  MIGRATION_FILE: ${{ inputs.migration_file || '' }}

jobs:
  trigger-migrate:
    name: Applisér migrasjoner mot Render-backend
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Deploy eksakt hotfix-commit til Render
        env:
          RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}
        run: |
          if [ -z "$RENDER_API_KEY" ]; then
            echo "::error::RENDER_API_KEY secret er ikke satt"
            exit 1
          fi
          echo "::add-mask::$RENDER_API_KEY"
          REQUEST_BODY=$(python3 -c 'import json,os; print(json.dumps({"commitId": os.environ["GITHUB_SHA"], "clearCache": "do_not_clear"}))')
          HTTP_CODE=$(curl -sS -o /tmp/render_deploy.json --connect-timeout 5 -m 30 -w "%{http_code}" \
            -X POST \
            -H "Accept: application/json" \
            -H "Authorization: Bearer $RENDER_API_KEY" \
            -H "Content-Type: application/json" \
            -d "$REQUEST_BODY" \
            "https://api.render.com/v1/services/$RENDER_SERVICE_ID/deploys")
          echo "Render deploy-trigger HTTP=$HTTP_CODE"
          if [ "$HTTP_CODE" != "200" ] && [ "$HTTP_CODE" != "201" ]; then
            cat /tmp/render_deploy.json
            echo "::error::Render avviste eksakt commit-deploy."
            exit 1
          fi

      - name: Vent til commit er deployet + stabil (unngår 502 i deploy-rollover)
        run: |
          echo "Venter til $GITHUB_SHA er live på $BACKEND_URL …"
          for i in $(seq 1 60); do   # opptil 60 × 15s = 15 min
            DEPLOYED=$(curl -s -m 10 "$BACKEND_URL/api/version" | python3 -c "import sys,json;print(json.load(sys.stdin).get('commit') or '')" 2>/dev/null || echo "")
            echo "  [$i] deployet commit: ${DEPLOYED:-<ukjent>}"
            if [ -n "$DEPLOYED" ] && { [ "$DEPLOYED" = "$GITHUB_SHA" ] || [ "${GITHUB_SHA#$DEPLOYED}" != "$GITHUB_SHA" ] || [ "${DEPLOYED#$GITHUB_SHA}" != "$DEPLOYED" ]; }; then
              echo "✅ Riktig commit er deployet."
              # Ekstra buffer så deploy-rolloveren er helt ferdig.
              sleep 15
              exit 0
            fi
            sleep 15
          done
          echo "::error::Tidsavbrudd — så ikke $GITHUB_SHA på /api/version. Migrering startes ikke."
          exit 1

      - name: Avgrens til én eksplisitt migreringsfil
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          if [ -z "$MIGRATION_FILE" ] && [ "$GITHUB_EVENT_NAME" = "push" ]; then
            MIGRATION_FILE=$(curl -fsS \
              -H "Authorization: Bearer $GITHUB_TOKEN" \
              -H "Accept: application/vnd.github+json" \
              "$GITHUB_API_URL/repos/$GITHUB_REPOSITORY/commits/$GITHUB_SHA" | \
              python3 -c 'import json,sys; files=[f["filename"].split("/")[-1] for f in json.load(sys.stdin).get("files",[]) if f.get("status") != "removed" and f.get("filename","").startswith("backend/migrations/") and f.get("filename","").endswith(".sql")]; print("\n".join(files))')
          fi
          FILE_COUNT=$(printf '%s\n' "$MIGRATION_FILE" | sed '/^$/d' | wc -l | tr -d ' ')
          if [ "$FILE_COUNT" != "1" ] || [[ ! "$MIGRATION_FILE" =~ ^[A-Za-z0-9][A-Za-z0-9._-]*\.sql$ ]]; then
            echo "::error::Workflowen krever nøyaktig én trygg .sql-fil; fant $FILE_COUNT. Kjør manuelt én fil om gangen."
            exit 1
          fi
          echo "MIGRATION_FILE=$MIGRATION_FILE" >> "$GITHUB_ENV"
          echo "🎯 Migreringsscope: $MIGRATION_FILE"

      - name: Bekreft migrasjons-auth (uten databasearbeid)
        env:
          MIGRATE_TRIGGER_TOKEN: ${{ secrets.MIGRATE_TRIGGER_TOKEN }}
        run: |
          MIGRATE_TOKEN=$(printf '%s' "$MIGRATE_TRIGGER_TOKEN" | tr -d '\r\n')
          HTTP_CODE=$(curl -sS -o /tmp/auth_body.txt --connect-timeout 3 -m 8 -w "%{http_code}" \
            -H "x-cron-trigger-token: $MIGRATE_TOKEN" \
            "$BACKEND_URL/api/admin-room/migrations/auth-check")
          echo "Auth-check HTTP=$HTTP_CODE body=$(cat /tmp/auth_body.txt 2>/dev/null)"
          if [ "$HTTP_CODE" != "200" ]; then
            echo "::error::Migrasjons-auth feilet før databasearbeid (HTTP $HTTP_CODE)."
            exit 1
          fi

      - name: Trigger migrate.sh
        env:
          MIGRATE_TRIGGER_TOKEN: ${{ secrets.MIGRATE_TRIGGER_TOKEN }}
        run: |
          if [ -z "$MIGRATE_TRIGGER_TOKEN" ]; then
            echo "::error::MIGRATE_TRIGGER_TOKEN secret er ikke satt"
            exit 1
          fi
          # CR/LF må fjernes før secret-en bygges inn i en HTTP-header.
          MIGRATE_TOKEN=$(printf '%s' "$MIGRATE_TRIGGER_TOKEN" | tr -d '\r\n')
          if [ -z "$MIGRATE_TOKEN" ]; then
            echo "::error::MIGRATE_TRIGGER_TOKEN er tom etter normalisering"
            exit 1
          fi
          # Fire-and-confirm: /run svarer før migrate.sh startes. En kort,
          # ikke-fatal timeout beskytter likevel mot nettverksfeil; status-
          # pollingen under er autoritativ.
          set +e
          REQUEST_BODY='{}'
          if [ -n "$MIGRATION_FILE" ]; then
            if [[ ! "$MIGRATION_FILE" =~ ^[A-Za-z0-9][A-Za-z0-9._-]*\.sql$ ]]; then
              echo "::error::Ugyldig migration_file-input."
              exit 1
            fi
            REQUEST_BODY=$(python3 -c 'import json,os; print(json.dumps({"migrationFile": os.environ["MIGRATION_FILE"]}))')
          fi
          HTTP_CODE=$(curl -s -o /tmp/run_body.txt --connect-timeout 3 -m 15 -w "%{http_code}" \
            -X POST \
            -H "Content-Type: application/json" \
            -H "x-cron-trigger-token: $MIGRATE_TOKEN" \
            -H "X-Migrate-Trigger-Source: github-actions/$GITHUB_SHA" \
            -d "$REQUEST_BODY" \
            "$BACKEND_URL/api/admin-room/migrations/run")
          CURL_EXIT=$?
          set -e
          echo "curl-exit=$CURL_EXIT HTTP=${HTTP_CODE:-<ingen>}"
          echo "Body: $(cat /tmp/run_body.txt 2>/dev/null)"
          if [ "$CURL_EXIT" = "28" ] || [ -z "$HTTP_CODE" ] || [ "$HTTP_CODE" = "000" ]; then
            echo "::warning::Trigger-respons uteble (timeout/tapt). migrate.sh er sannsynligvis startet — går til status-polling."
          elif [ "$HTTP_CODE" = "202" ] || [ "$HTTP_CODE" = "200" ] || [ "$HTTP_CODE" = "409" ]; then
            echo "Migrate trigget (eller pågår alt). Poller status."
          else
            echo "::error::Migrate-trigger avvist (HTTP $HTTP_CODE) — sjekk at MIGRATE_TRIGGER_TOKEN-secret matcher backend-env."
            exit 1
          fi

      - name: Poll status til ferdig (token-auth, maks 20 min)
        env:
          MIGRATE_TRIGGER_TOKEN: ${{ secrets.MIGRATE_TRIGGER_TOKEN }}
        run: |
          # pendingCount er autoritativt (DB: migrations/*.sql minus
          # _migrations_applied) og overlever en evt. instans-restart som
          # nullstiller in-memory status. Suksess = ingen pending igjen.
          MIGRATE_TOKEN=$(printf '%s' "$MIGRATE_TRIGGER_TOKEN" | tr -d '\r\n')
          STATUS_URL="$BACKEND_URL/api/admin-room/migrations/status"
          if [ -n "$MIGRATION_FILE" ]; then
            STATUS_URL="$STATUS_URL?migrationFile=$MIGRATION_FILE"
          fi
          DEADLINE=$((SECONDS + 1200))
          i=0
          while [ "$SECONDS" -lt "$DEADLINE" ]; do
            i=$((i + 1))
            set +e
            STATUS_JSON=$(curl -s --connect-timeout 3 -m 8 -H "x-cron-trigger-token: $MIGRATE_TOKEN" "$STATUS_URL")
            CURL_EXIT=$?
            set -e
            if [ "$CURL_EXIT" != "0" ]; then STATUS_JSON='{}'; fi
            STATUS=$(echo "$STATUS_JSON" | python3 -c "import sys,json;print(json.load(sys.stdin).get('status',''))" 2>/dev/null || echo "")
            PENDING=$(echo "$STATUS_JSON" | python3 -c "import sys,json;print(json.load(sys.stdin).get('pendingCount',''))" 2>/dev/null || echo "")
            PENDING_CHECK=$(echo "$STATUS_JSON" | python3 -c "import sys,json;print(json.load(sys.stdin).get('pendingCheck',''))" 2>/dev/null || echo "")
            echo "  [$i] status=$STATUS pending=$PENDING pendingCheck=$PENDING_CHECK curlExit=$CURL_EXIT"
            if [ "$STATUS" = "failed" ]; then
              echo "Detaljer: $STATUS_JSON"
              echo "::error::Migrate-run FEILET. Se Admin Room → Database-migrasjoner → Vis logg."
              exit 1
            fi
            # Ferdig når ingen migrasjoner er pending (autoritativt fra DB).
            if [ "$PENDING" = "0" ] && [ "$STATUS" != "running" ]; then
              echo "✅ Migrate ferdig — ingen pending migrasjoner igjen (status=$STATUS)."
              exit 0
            fi
            # migrate.sh fortsetter per design etter en SQL-feil. Når child-
            # prosessen er avsluttet, men DB fortsatt rapporterer pending, vil
            # videre polling aldri endre resultatet. Feil raskt og ta med hele
            # statusobjektet (pendingFiles + siste migreringslogg) i Actions.
            if [ "$STATUS" != "running" ] && [ -n "$PENDING" ] && [ "$PENDING" != "0" ]; then
              echo "Detaljer: $STATUS_JSON"
              echo "::error::Migrate-run avsluttet med $PENDING uanvendte migrasjoner."
              exit 1
            fi
            sleep 15
          done
          echo "::error::Tidsavbrudd på status-polling. Migrasjoner er ikke bekreftet ferdige."
          exit 1
