# GPU variant of requirements.txt — CUDA 12.1 torch so GFPGAN/Real-ESRGAN run
# on the GPU (GFPGANer auto-selects cuda when available). Used by Dockerfile.gpu
# (RunPod / Render-GPU / Fly GPU). The Modal path (modal_app.py) pins the same
# versions inline.
--extra-index-url https://download.pytorch.org/whl/cu121

fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.4
boto3==1.35.90
botocore==1.35.90
numpy==1.26.4
Pillow==10.4.0
opencv-python-headless==4.10.0.84
mediapipe==0.10.18
torch==2.1.2
torchvision==0.16.2
basicsr==1.4.2
facexlib==0.3.0
realesrgan==0.3.0
gfpgan==1.3.8
# RunPod Serverless SDK — the serverless endpoint reuses THIS public image
# (ghcr.io/creaotrhubn26/gfpgan-runner:latest) with a dockerStartCmd override
# to ``python -u rp_handler.py`` instead of uvicorn, so no separate (private)
# image/package is needed. Harmless to the HTTP runner, which keeps uvicorn.
runpod==1.7.7
